# Open Questions

Each item remains unconfirmed until a reproduction or authoritative semantic
reference is recorded.

- Does the static evaluator's undirected MST produce a number presented as
  directed executable campaign depth?
- Can node properties used by `match_properties` be synthesized outside
  `identifiers.base_properties` and `standard_ports`?
- Does deleting and regenerating an exact scenario slot leave stale manifest,
  image, report, or metrics references?
- Are local privilege-escalation outcomes intentionally excluded from the static
  action planner because goal ownership alone is sufficient, or does this erase
  required System-level terminal conditions?
- Do resume checks accept old `run_metrics.json` generated under a different
  config hash or validator version?
- Does any external wrapper persist random seeds and hashes outside each scenario
  directory? The in-repository scenario writer does not do so.
