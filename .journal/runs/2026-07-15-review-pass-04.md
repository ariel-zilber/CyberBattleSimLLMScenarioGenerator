# Review Pass 04 — Gate Integrity

## Trace completed

Mapped Phase 0 through Step 9 in `pipeline/run.py`, focusing on what can mutate
the data and when metrics are invalidated.

## Confirmed

1. Nonzero BFS execution is always called partial solve rather than classified.
2. Goal completeness is evaluated from the snapshot collected before unsolved
   scenario replacement.
3. Expanded topology YAML is produced after validation and is never instantiated
   or dynamically validated.
4. Coverage failure does not prevent overall pipeline success.

## Data-integrity invariant needed

For every reported metric, preserve and verify this binding:

```text
metric -> evaluator version -> scenario content hash -> generator config hash
       -> vocabulary hash -> acceptance policy
```

Any scenario mutation or config switch invalidates downstream metrics until that
binding is recomputed.
