# Review pass 19 — object compiler identity, paths, and stale output

Scope: read-only parser/compiler review and isolated `/tmp` reproductions. Only
Markdown under `.journal` was retained.

## Confirmed findings

1. Legacy DSL scenario/node names are unsanitized filesystem path components.
2. Recompiling to the same scenario directory retains removed nodes.
3. A declared `start` node validates and BFS-solves but is overwritten by the
   synthetic attacker during compilation.
4. `/24` allocation reaches broadcast and then out-of-subnet addresses after
   245 usable offsets because no capacity check exists.
5. Duplicate node declarations silently collapse to the last declaration.

## Reproduction evidence

- `../escaped` passed validation and created `safe/escaped.yaml`, not a file
  contained by `safe/nodes/`.
- Second compilation retained `same/nodes/old.yaml`.
- Compiled colliding start: image `attacker`, properties `[breach_node]`, empty
  services, despite a different validated user declaration.
- Address sequence included `10.1.0.255` and then `10.1.1.0` under declared
  subnet `10.1.0.0/24`.
- Duplicate Lark node `x` retained the later `database` template and validation
  remained successful.

Temporary reproduction files were deleted after inspection.
