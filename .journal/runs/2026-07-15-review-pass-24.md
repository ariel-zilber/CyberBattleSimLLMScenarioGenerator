# Review pass 24 — adversarial verification and correction

Triggered by the user's concern that many findings may not be real. Expansion of
the issue list was paused; high-severity claims are now being tested against the
actual improved actuator where possible.

## Corrections

1. The remote-exploit firewall claim was already withdrawn: runtime also ignores
   firewall for remote vulnerability exploits.
2. The preferred object's claimed numeric 9-to-8 depth collapse is withdrawn.
   Improved-runtime execution took 9 actions because compilation adds an initial
   discovery action while bypassing the Admin action.
3. Problem 70 is downgraded P0 to P1. The missing Admin gate and path mismatch are
   dynamically confirmed; numeric depth collapse is not confirmed by this case.

## Runtime evidence retained

- The object artifact is not natively loader-complete; its README documents that
  exact environment replay is outside the MVP.
- With a temporary identifier adapter, all compiled nodes loaded into the actual
  improved actuator.
- At dc/User, `SystemEscalation` executed successfully without
  `DomainAdminEscalation` and raised dc to System.
- The full executed sequence contained 9 actions.

## Verification policy going forward

Items must be labeled separately as dynamically reproduced, directly reproduced
at component level, source-confirmed semantic mismatch, documented limitation,
or hypothesis. Unsupported runtime claims will be downgraded or withdrawn.
