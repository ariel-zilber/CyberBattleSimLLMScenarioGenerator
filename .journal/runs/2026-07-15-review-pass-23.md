# Review pass 23 — object-generator provenance

Identical preferred Lark input was compiled under `PYTHONHASHSEED=1` and `2` in
temporary directories.

## Result

- Scenario hashes differed.
- Report YAML reordered properties, services, and prerequisites.
- Source inspection confirmed the digest covers the spec serialization only,
  not the compiled runtime files.

The temporary outputs were deleted. No project implementation or data changed.
