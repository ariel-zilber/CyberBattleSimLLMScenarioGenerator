# Review pass 20 — transition fields versus CyberBattleSim runtime

Scope: read-only comparison of object BFS/compiler fields against the connected
CyberBattleSim actuator, plus in-memory compilation probes.

## Correction

The previously logged remote-exploit firewall mismatch is withdrawn. The actual
runtime does not firewall-gate remote vulnerability exploits; it firewall-gates
credential Connect actions. The journal index now marks item 57 withdrawn.

## Confirmed

1. RemoteExploit and Connect can grant Admin/System in BFS, but their runtime
   outcomes grant only LocalUser.
2. A transition with success rate 0 validates and is certified as a deterministic
   one-step solution.
3. Probe is emitted as a source-stored remote vulnerability using properties
   validated on the target.
4. DSL `requires_privilege` gates BFS but is not represented by compiled
   source-privilege enforcement, allowing runtime shortcuts.

## Direct evidence

- Zero-rate case: validation true, BFS solved depth 1, compiled successRate 0.0.
- System Connect case: validation true, BFS solved depth 1; compiled goal
  privilege remained 0 and no grant outcome existed.
- Probe case: validation true; source properties empty; source-held type-3
  vulnerability precondition and outcome both referenced `Secret`.
- Runtime actuator source: both LateralMove and credential Connect invoke
  ownership with its default LocalUser privilege.
