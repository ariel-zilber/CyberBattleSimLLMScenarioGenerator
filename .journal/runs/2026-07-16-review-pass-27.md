# Review pass 27 — specialist training logs

Read-only audit of the full lateral and live Windows seed-42 logs.

Confirmed:

- exact duplicated episodes 501–607 in both CSVs after resume;
- BFS fields disappear after the early/resumed segment and checkpoint BFS
  denominators are zero;
- lateral is meaningfully above random (28% versus 4%) but has a large
  train/held-out gap and 91.7% invalid evaluation actions;
- Windows peaks near checkpoint 4,000 and later invalid-action/ranking metrics
  degrade.

Correction: the initial interpretation called lateral unhealthy and advised
rejecting it. That conclusion exceeded the evidence. Unmasked greedy evaluation
is intentional, and 7× random-baseline success proves learning. The journal now
reports comparative limitations without declaring the model bad.

No run file, checkpoint, model, source file, or generated scenario was changed.
