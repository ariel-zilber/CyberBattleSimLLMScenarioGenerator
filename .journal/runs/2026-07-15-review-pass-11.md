# Review Pass 11 — Depth Certification

## Result

The current generation order invalidates the meaning of “certified.” Violations
do not block, the certificate is not persisted, and a coverage sweep mutates the
graph afterward.

## Direct evidence

The deterministic probe from Pass 10 produced a successful output directory and
exit code zero while reporting a depth violation for one goal.
