# Review pass 22 — advertised object depth certificate

The repository's preferred Lark example was compiled in `/tmp` and examined
without changing project files.

## Result

The intended exact path is not preserved by compilation. Both Admin and System
escalations receive the same static property precondition, so System is available
at User and the Admin step is bypassed. The compiled condition proof confirmed
the bypass, while the CLI incorrectly reported no bypassable mandatory transition.

Correction from the subsequent improved-runtime adapter run: total executed
actions remained 9 because compiled initial discovery adds one action while the
missing Admin gate removes one. This fixture confirms semantic path mismatch but
does not establish a numeric 9-to-8 collapse.

This is the combined effect of:

- missing compilation of `requires_privilege` (problem 70), and
- mandatory-bypass analysis examining only one selected BFS path (problem 61).

The condition solver also ignores success rates, so it cannot serve as a backstop
for zero-probability paths (problem 71).
